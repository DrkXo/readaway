/// flutter_gen only generates `assets`/`fonts`/`colors`, not the
/// `shaders` section, so shader paths stay here.
class AppShaders {
  const AppShaders._();

  static const tactileSurface = 'assets/shaders/tactile_surface.frag';
}
