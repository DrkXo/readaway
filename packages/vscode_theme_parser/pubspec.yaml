name: vscode_theme_parser
description: "Pure Dart parser and model for VS Code color themes (JSONC)."
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  freezed_annotation: ^3.0.0
  json_annotation: ^4.12.0
  jsonc: ^0.0.3

dev_dependencies:
  build_runner: ^2.10.0
  mockito: ^5.4.5
  freezed: ^4.0.1
  json_serializable: ^6.8.0
  lints: ^6.0.0
  test: ^1.25.0