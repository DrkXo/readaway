name: mupdf
description: MuPDF FFI bindings for Flutter — PDF, XPS, EPUB rendering.
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  ffi: ^2.1.0

dev_dependencies:
  ffigen: ^21.0.0
